# Contexto do Projeto - Front-end (EPI SaaS / Solicitação OS)

## Regra de Autonomia Absoluta
- **Execução Direta:** Nunca peça confirmações óbvias, validações intermediárias ou permissões para prosseguir com o que foi solicitado. Se o comando foi dado, execute a tarefa por completo, gere o código necessário de ponta a ponta e apresente o resultado final de forma autônoma.

## Regra de Integração e Navegação Real
- **Simulação de Aplicação Real:** Nenhuma tela deve ser tratada como um componente isolado ou estático. Todas as telas recém-criadas DEVEM ser interligadas de verdade através do React Router DOM, permitindo a navegação completa de ponta a ponta (ex: clicar em "Minhas Solicitações" na Home direciona para a tela de listagem, clicar em "Nova OS" abre o formulário, etc.).
- **Estado de Autenticação / Mock de Sessão:** Utilize um contexto ou estado global leve (Zustand) para simular o perfil ativo (Solicitante, Técnico, Gestor, Administrador) e o nome do usuário logado, garantindo que o fluxo de login redirecione corretamente para o painel correspondente de forma dinâmica.

## Tech Stack e Infraestrutura
- **Framework & Build:** React 19+ com TypeScript, empacotado via **Vite**.
- **Roteamento:** React Router DOM (com implementação de Rotas Protegidas).
- **Estilização:** Tailwind CSS (Mobile-First obrigatório).
- **Ícones:** Lucide React.
- **Gerenciamento de Formulários:** React Hook Form integrado com **Zod** para validação de esquemas.
- **Busca de Dados & Cache:** TanStack Query (React Query) para chamadas HTTP, cache de listas e painéis de indicadores (dashboards).
- **Gerenciamento de Estado Global:** Zustand (ou Context API nativa) com foco principal no controle de Modais globais.
- **Qualidade de Código (Linting & Formatting):** ESLint e Prettier (utilizando o plugin `prettier-plugin-tailwindcss` para ordenação automática das classes).
- **Cliente HTTP:** Wrapper customizado nativo (Fetch API), substituindo bibliotecas de terceiros como Axios.
- **Notificações/Alertas:** React-Toastify (Para feedback global de erros, sucesso e avisos).

## Estrutura de Pastas e Organização do Código
A arquitetura do projeto deve seguir uma separação clara de responsabilidades e **adotar estritamente a nomenclatura em Português-BR**:
- **Idioma do Código (Português-BR):** Nomenclaturas de pastas, componentes, funções, variáveis e arquivos devem ser em Português (ex: `Botao` em vez de `Button`, `formatarData` em vez de `formatDate`). Apenas prefixos/termos nativos do React (como `use` para hooks) devem ser mantidos.
- **Modularização e Limite de Tamanho (REGRA DE OURO):** Evite arquivos gigantes. Se um arquivo começar a ficar muito extenso e complexo, ele **DEVE** ser dividido em partes menores (criação de subcomponentes, divisão lógica de arquivos ou extração de regras de negócio para hooks/utilitários independentes).
- **`/src/componentes`**: Componentes visuais isolados e reaproveitáveis (ex: `Botao`, `CampoTexto`, `CardMaquina`). Devem ser "burros" (receber apenas props) sempre que possível.
- **`/src/paginas`** (ou `/telas`): Telas completas da aplicação (ex: `PainelGestor.tsx`, `TelaLogin.tsx`). Montagem de layout e consumo de hooks/queries. Se a página for muito complexa, crie uma subpasta para ela com seus componentes locais (ex: `/src/paginas/PainelGestor/componentes/`).
- **`/src/servicos`**: Configuração do cliente HTTP customizado e abstração de endpoints (ex: `api.ts`, `servicoMaquinas.ts`).
- **`/src/hooks`**: Ganchos customizados, incluindo os do React Query (ex: `useMaquinas.ts`, `useAutenticacao.ts`).
- **`/src/estado`**: Gerenciamento de estados globais (Zustand), especialmente para a **Máquina de Modais** (evitando propagação excessiva de `useState` entre componentes).
- **`/src/utilitarios`**: Funções puras (ex: `formatarData.ts`, `formatarMoeda.ts`).
- **`/src/rotas`**: Configuração do roteamento, separando rotas públicas das rotas protegidas por perfil (Gestor, Técnico, Solicitante, Administrador).
- **`/src/tipos`**: Tipagens estritas do TypeScript (interfaces refletindo o banco de dados PostgreSQL).

## Autenticação, Segurança e Regras de Negócio (IMPORTANTE)
- **Arquitetura Multi-Locatário (Multi-Tenant) Baseada em Subdomínio:** O sistema atende a múltiplos clientes. O `tenantId` é extraído dinamicamente do subdomínio da URL atual (`window.location.hostname.split(".")[0]`) e deve ser injetado em todas as requisições via header estrito `X-tenant-ID`.
- **Gerenciamento de Sessão via Cookies:** O token JWT retornado pelo back-end é gerenciado via **Cookies HttpOnly**. É terminantemente proibido armazenar o JWT em `localStorage` ou variáveis globais. O cliente HTTP deve usar `credentials: "include"`.
- **Tratamento Global de Erros (401 Não Autorizado):** Se a API retornar status 401 e a rota atual não for `/login`, o sistema deve interceptar a falha, disparar um alerta via React-Toastify ("Sua sessão expirou") e redirecionar o usuário forçosamente para o Login.
- **Hierarquia Organizacional (Tenant > Loja > Setor):** Cada tenant (cliente SaaS, identificado pelo subdomínio) pode possuir múltiplas Lojas (unidades/filiais), e cada Loja possui seus próprios Setores. Máquinas e Ordens de Serviço pertencem sempre a uma Loja + Setor específicos (`lojaId` + `setor`), definidos em `/src/tipos/loja.ts` e `/src/tipos/maquina.ts`.
- **Controle de Acesso por Loja e Setor (Gestor):** O acesso do Gestor é definido por uma lista de **escopos** (`EscopoAcessoGestor[]`, em `/src/tipos/autenticacao.ts`), onde cada escopo vincula uma Loja a um conjunto de setores. Um escopo pode ser:
  - **Restrito a setores específicos** dentro de uma Loja (`setores: Setor[]`) — ex: Gestor que responde apenas pelos setores "Padaria" e "Açougue" da Loja 1, enquanto outro Gestor da mesma loja responde por "Hortifruti" e "Peixaria".
  - **Total sobre uma Loja inteira** (`setores: 'todos'`) — ex: Gestor que responde por todos os setores das Lojas 2 e 3 simultaneamente (um Gestor pode ter múltiplos escopos, cobrindo múltiplas lojas ao mesmo tempo).
  O sistema garante que o Gestor só visualize OS/máquinas dentro dos seus escopos (`gestorTemAcesso` / `filtrarPorAcessoGestor` em `/src/utilitarios/acessoGestor.ts`), criando separadores visuais na listagem agrupados por Loja e, dentro dela, por Setor quando múltiplos estiverem envolvidos.

- **Controle de Acesso por Setor (Solicitante):** O Solicitante está vinculado a exatamente uma Loja + um Setor (`lojaId`/`setor` em `useEstadoAutenticacao`, definidos no momento do login). Ao abrir uma Nova Solicitação OS, ele só pode visualizar e selecionar **máquinas do seu próprio setor** — ex: um solicitante do Açougue só vê as máquinas do Açougue, nunca as da Padaria ou de outra loja. O filtro é aplicado via `useMaquinas({ setor, lojaId })`, que repassa os parâmetros para `servicoMaquinas.listar`.
- **Máquina exige Manutenção Preventiva (REGRA DE NEGÓCIO):** uma Máquina só pode ser cadastrada (ou ter seu cadastro salvo em edição) se tiver **uma ou mais Manutenções Preventivas** vinculadas no mesmo formulário — o envio é bloqueado sem isso, validado via Zod (`esquemaCadastrarMaquina`, `preventivas` com `min(1)`). Ver detalhamento em `CadastrarMaquina` (item 5).
- **Acesso Total do Administrador (4º perfil):** O Administrador (`PerfilLogin` inclui `'administrador'` em `/src/tipos/autenticacao.ts`) tem acesso irrestrito a **todo o tenant**, sem o conceito de escopo por Loja/Setor que existe para o Gestor — não há `EscopoAcessoGestor` nem `lojaId`/`setor` associados a ele em `useEstadoAutenticacao`. É o perfil responsável por **todas as adições/cadastros do sistema** (Usuários, Lojas, Setores, Técnicos e Máquinas), com CRUD completo (criar, listar, atualizar, excluir) em cada um — ver item 12.
- **Rotas Protegidas por Perfil:** `RotaProtegida` (`/src/rotas/RotaProtegida.tsx`) aceita uma prop opcional `perfis?: PerfilLogin[]`. Sem autenticação, redireciona para `/login`; autenticado mas com perfil fora da lista `perfis`, redireciona para a rota inicial do próprio perfil (`ROTA_POR_PERFIL[perfil]`) em vez de deixar renderizar. Em `RotasPrincipais.tsx`, as rotas de Solicitante/Técnico (`/home-solicitante`, `/nova-solicitacao-os`, `/minhas-solicitacoes`) usam `perfis={['solicitante', 'tecnico']}`; as de Gestor (`/painel-gestor`, `/dashboard-gestor`) usam `perfis={['gestor']}`; e as de Administrador (`/painel-administrador`, `/administrador/usuarios`, `/administrador/lojas`, `/administrador/setores`, `/administrador/tecnicos`, `/administrador/maquinas`, `/administrador/custos-pendentes`, além das telas de cadastro `/cadastrar-usuario`, `/cadastrar-loja`, `/cadastrar-setor`, `/cadastrar-maquina` — cada uma com uma rota irmã `/:id` para o modo de edição) usam `perfis={['administrador']}` — evitando que, por exemplo, um Solicitante acesse o Painel do Gestor ou o Painel do Administrador digitando a URL diretamente.

## Configuração Base de API (`api.ts`) & Variáveis de Ambiente
Todo o tráfego HTTP passa por um wrapper nativo (`fetch`) padronizado.
- **Variáveis de Ambiente:** O projeto deve conter um arquivo `.env.example` na raiz documentando as variáveis necessárias (ex: `REACT_APP_URL_API`).
- O script `api.ts` utiliza `process.env.REACT_APP_URL_API` com fallback para a URL de homologação. Garante formatação com `https://` caso omitido.
- **Cabeçalhos Automáticos:** Injeta `Content-Type: application/json` (exceto quando o corpo da requisição for `FormData` para uploads) e o `X-tenant-ID`.
- **Resolução de Resposta:** Lê corretamente respostas JSON, Texto ou arquivos Binários/Blob (como PDF para impressão de OS).
- **Integração de Alertas:** Captura chaves de erro do backend (`error`, `erro`, `message`, `detalhes`) e exibe o toast (notificação) automaticamente.

## Especificações de Telas

### 1. Tela de Login (`TelaLogin`)
- **Fundo Global:** Gradiente suave em tons de verde escuro.
- **Card Central:** Fundo branco, cantos arredondados (`rounded-2xl`), padding generoso interno, largura fixa elegante (`max-w-md`), com sombra suave.
- **Cabeçalho do Card:** Título "Solicitação OS" centralizado, negrito e escuro. Subtítulo "LOGIN DE ACESSO" em letras maiúsculas, menor e em tom de verde claro/cinza.
- **Labels dos Campos:** Todas as labels ("PERFIL", "LOGIN", "SENHA DE ACESSO") devem ser em letras maiúsculas, fonte pequena (`text-xs`), peso médio e cor verde/cinza clara.
- **Seletor de Perfil (Tabs):** Fundo do container em tom muito claro de cinza/verde, cantos arredondados (`rounded-2xl`), contendo ícones da biblioteca Lucide ao lado do texto (`User` Solicitante, `Wrench` Técnico, `ShieldCheck` Gestor, `UserCog` Administrador — 4 abas, componente `SeletorPerfil` compartilhado entre a Tela de Login e o `CadastrarUsuario`, ver item 12). A aba ativa deve ter fundo verde escuro e texto branco. Abas inativas com fundo transparente e texto verde. **Layout fixo em grade 2×2 (`grid grid-cols-2`), não por breakpoint de viewport:** o seletor sempre vive dentro de um card estreito (`max-w-md`), então o espaço disponível não cresce com o tamanho da tela — uma tentativa anterior de alternar para uma linha única a partir do breakpoint `sm` (`sm:flex`) cortava o texto das abas em telas largas, porque `sm` reage à largura da *viewport*, não à largura do *card* (que é sempre ~448px independente do monitor). A grade 2×2 fixa resolve isso em qualquer tamanho de tela.
- **Campos de Input (E-mail e Senha):** Devem possuir um fundo amarelado/esverdeado muito claro (ex: `bg-lime-50` ou similar), sem bordas fortes, cantos arredondados (`rounded-lg`), texto em verde escuro. O ícone de visibilidade da senha deve ser verde.
- **Link Auxiliar:** "Esqueci minha senha" alinhado à direita, com fonte pequena e cor verde.
- **Botão Principal:** Botão "Entrar" com fundo verde escuro sólido, texto branco e cantos arredondados.
- **Rodapé do Card:** Texto "SOLICITAÇÃO OS © 2026" centralizado na base do card, fonte muito pequena e cor cinza clara.

### 2. Tela Principal do Solicitante (`HomeSolicitante`)
- **Fundo Global:** Cinza sólido uniforme (`bg-slate-600`), estruturado de ponta a ponta (header superior verde, conteúdo centralizado e footer discreto).
- **Header Superior:** Faixa em verde escuro alinhada de ponta a ponta. Contém no canto esquerdo o título "SOLICITAÇÃO OS" em destaque pequeno e logo abaixo a saudação personalizada ("Olá, `nome do usuario logado`"). No canto direito, ficam os ícones de Notificações (sino) e Sair (logout).
- **Área Central:** Título principal centralizado "O que deseja fazer?" acompanhado do subtítulo descritivo.
- **Cards de Ação (Esquema de Cores Obrigatório):** 
  - Os botões/cards de navegação centralizados em largura moderada.
  - O card "Minhas Solicitações" possui fundo cinza escuro secundário (`bg-slate-700`) com ícone e texto explicativo.
  - O card "Nova Solicitação OS Maquinário" possui destaque preenchido em verde sólido (`bg-emerald-600`) com ícone de adição (`CirclePlus`) — leva para o formulário de OS de máquina cadastrada (item 3).
  - O card "Nova Solicitação OS Pequenos Reparos" possui uma cor própria em laranja (`bg-orange-600`, ícone `Hammer`) para se diferenciar visualmente da OS de Maquinário sem reaproveitar o âmbar já usado como cor de "alerta"/pendência em outras telas — leva para o formulário de pequenos reparos (item 3b). `CardAcao` (`/src/componentes/CardAcao.tsx`) ganhou uma terceira `variante: 'reparo'` para isso, além de `'padrao'` e `'destaque'`.
  - O card "Nova Solicitação OS Terceiros" possui uma cor própria em azul (`bg-blue-600`, ícone `Truck`) para reforçar o caráter "externo/parceiro" do atendimento, sem repetir verde (Maquinário), laranja (Pequenos Reparos) ou âmbar (alerta) — leva para o formulário de OS atendida por empresa terceirizada (item 3c). `CardAcao` ganhou uma quarta `variante: 'terceiros'` para isso.
- **Cards de Estatísticas (Grid Inferior):** Três pequenos blocos cinzas lado a lado exibindo os contadores numéricos centralizados com seus respectivos rótulos abaixo (`Abertas`, `Em andamento`, `Concluídas`).
- **Rodapé:** Texto discreto centralizado na base `SOLICITAÇÃO OS © 2026`.

### 3. Tela de Nova Solicitação OS Maquinário (`NovaSolicitacaoOS`)
- **Formulário Complexo (Zod + React Hook Form):** 
  - **Máquina:** Seleção atrelada a uma pré-visualização de foto (`object-contain`). A lista é restrita apenas às máquinas do setor/loja do solicitante logado.
  - **Tipo de Defeito:** Seleção fixa (`Mecânico`, `Elétrico`, `Hidráulico`, `Pneumático`, `Software / CNC`, `Estrutural`).
  - **Setor / Solicitante:** Preenchimento automático (somente leitura).
  - **Data/Hora:** Preenchimento automático e somente leitura (`new Date().toISOString()` capturado uma única vez, via `useState`, na abertura da tela — mesmo padrão de "confirmação de instante" já usado no `ModalAbrirOrdemServico`/`ModalEncerrarOrdemServico`, itens 10/11). Faz parte de `NovaSolicitacaoOSPayload` (`/src/tipos/ordemServico.ts`, campo `dataHora`).
  - **Descrição:** Área de texto (`textarea`) com validação de limite de caracteres.
  - **Foto do Defeito \* (REGRA DE NEGÓCIO):** obrigatória — captura via `UploadFoto` (`/src/componentes/UploadFoto.tsx`, `capture="environment"` no input para abrir a câmera nativa direto no celular). Diferente da pré-visualização da Máquina (que mostra a foto já cadastrada), essa é uma foto nova, tirada na hora, mostrando o defeito constatado. É controlada fora do React Hook Form (estado local `useState<File | null>`, mesmo padrão do `foto` em `NovaSolicitacaoReparo`) e validada no `aoEnviar` — sem ela, o envio é bloqueado com um toast de erro.
  - **Vídeo do Defeito (opcional):** captura via `UploadVideo` (`/src/componentes/UploadVideo.tsx`, novo componente espelhando o `UploadFoto` mas para vídeo — preview com `<video controls>`, também com `capture="environment"`). Útil quando o defeito é mais fácil de entender em movimento (ruído, vazamento, vibração).
  - **Marcadores de Impacto:** Caixas de seleção (checkbox) preenchidas pelo próprio Solicitante no momento do pedido (`Afeta Produção`, `Parada Parcial`, `Retrabalho`), definidas em `marcadoresImpacto`/`MarcadorImpacto` (`/src/tipos/ordemServico.ts`). Fazem parte de `SolicitacaoOS`/`NovaSolicitacaoOSPayload` e ficam visíveis para o Gestor ao visualizar a solicitação (ver item 6).
- **Ação:** Enviar Solicitação.
- **Desvio deliberado — mock-first:** `servicoSolicitacoes.criar` deixou de chamar `api.post` real e passou a empurrar a solicitação direto para `SOLICITACOES_MOCK` (mesmo padrão de Reparo/Terceiros, itens 3b/3c) — necessário para a Foto/Vídeo do Defeito aparecerem de verdade no `ModalDetalhesSolicitacao` do Gestor (item 6). A Foto do Defeito e o Vídeo viram `fotoUrl`/`videoUrl` na `SolicitacaoOS` via `URL.createObjectURL` (mesma estratégia de `CadastrarMaquina`, item 5).

### 3b. Tela de Nova Solicitação OS Pequenos Reparos (`NovaSolicitacaoReparo`)
- **Acesso:** card "Nova Solicitação OS Pequenos Reparos" na `HomeSolicitante` (item 2), rota `/nova-solicitacao-reparo`, mesmo grupo de rotas protegidas (`perfis={['solicitante', 'tecnico']}`) da OS de Maquinário.
- **Propósito:** cobre reparos pontuais que **não exigem cadastro prévio de um item no banco de dados** — lâmpada queimada, vidro quebrado, piso rachado, etc. Diferente da OS de Maquinário (item 3), não existe seleção de uma `Máquina` cadastrada: o Solicitante descreve o item na hora.
- **Cabeçalho colorido em laranja** (`bg-gradient-to-r from-orange-500 to-orange-600`) em vez do verde padrão das demais telas, reforçando a mesma diferenciação visual do card na Home (item 2).
- **Campos do Formulário (Validação Zod obrigatória):**
  - **Foto do Item:** upload de imagem reaproveitando o componente `UploadFoto` (movido de `CadastrarMaquina/componentes` para `/src/componentes/UploadFoto.tsx` por já ser usado em mais de uma tela — `rotulo`/`textoAlternativo` agora são props para customizar o texto por contexto).
  - **Item \***: texto livre (ex: "Lâmpada de LED") — não é um select vindo de uma lista cadastrada.
  - **Solicitante**: preenchimento automático (somente leitura).
  - **Setor**: preenchimento automático a partir do setor do Solicitante logado (`useEstadoAutenticacao`), somente leitura — diferente da OS de Maquinário, não depende de uma máquina selecionada.
  - **Data/Hora**: preenchimento automático e somente leitura, mesmo padrão do item 3.
  - **Descrição \***: área de texto com validação de limite de caracteres.
- **Serviço (`servicoReparos.ts`, tipo `NovaSolicitacaoReparoPayload` em `/src/tipos/reparo.ts`):** mock-first — constrói uma `SolicitacaoOS` com `tipo: 'reparo'` (`maquinaNome` recebe o texto livre do `item`, `maquinaCodigo` fica `'—'`) e empurra direto para `SOLICITACOES_MOCK` (`gerarProximoIdSolicitacao`, centralizado em `dadosMockSolicitacoes.ts` para não colidir com ids gerados por `servicoOSTerceiros`). A foto usa `URL.createObjectURL` (mesma estratégia de `CadastrarMaquina`, item 5) e vira `fotoUrl` na solicitação — não existe endpoint real de upload.
- **Integrado ao pipeline de aprovação (REGRA DE NEGÓCIO):** a partir daqui, o Pequeno Reparo segue o **mesmo fluxo da OS de Maquinário** — aparece na aba `Solicitações` do Gestor (item 6), que atribui um Técnico via `ModalAbrirOrdemServico` (item 10, o mesmo modal, sem alterações), passa pelos estados `Aberta`/`Em Andamento`/`Pausada`/`Concluída` no Painel do Técnico (item 9) e só vira "Finalizada" depois que o Administrador lança o custo de manutenção (item 12/13). Ainda **não aparece** em `Minhas Solicitações` (item 4) — isso continua sendo um passo futuro.

### 3c. Tela de Nova Solicitação OS Terceiros (`NovaSolicitacaoOSTerceiros`)
- **Acesso:** card "Nova Solicitação OS Terceiros" na `HomeSolicitante` (item 2), rota `/nova-solicitacao-os-terceiros`, mesmo grupo de rotas protegidas (`perfis={['solicitante', 'tecnico']}`) das demais OS.
- **Propósito:** cobre o caso em que uma máquina cadastrada quebra e o reparo **não é feito pelos técnicos internos**, mas por uma empresa terceirizada especializada (ex: assistência técnica de balanças, refrigeração comercial). Diferente da OS de Pequenos Reparos (item 3b), parte de uma `Máquina` já cadastrada — mesmo padrão de seleção com pré-visualização de foto (`PreviaMaquina`, movido de `NovaSolicitacaoOS/componentes` para `/src/componentes/PreviaMaquina.tsx` por já ser usado em mais de uma tela) e filtro pelo setor/loja do Solicitante logado (item 3).
- **Cabeçalho colorido em azul** (`bg-gradient-to-r from-blue-500 to-blue-600`), reforçando a mesma diferenciação visual do card na Home (item 2).
- **Campos do Formulário (Validação Zod obrigatória):**
  - **Máquina \***: select com pré-visualização de foto, restrito ao setor/loja do Solicitante (mesmo padrão do item 3).
  - **Tipo de Defeito \***: mesma seleção fixa do item 3 (`Mecânico`, `Elétrico`, `Hidráulico`, `Pneumático`, `Software / CNC`, `Estrutural`).
  - **Solicitante** e **Setor da Máquina**: preenchimento automático (somente leitura), derivado da máquina selecionada — mesmo padrão do item 3.
  - **Data/Hora**: preenchimento automático e somente leitura, mesmo padrão do item 3.
  - **Descrição \***: área de texto com validação de limite de caracteres.
  - **Foto do Defeito \*** e **Vídeo do Defeito (opcional)**: mesmos componentes e mesma regra do item 3 (`UploadFoto`/`UploadVideo`, foto obrigatória bloqueando o envio via toast se ausente, vídeo opcional) — a máquina foi enviada pra um terceiro, então a evidência visual do defeito é igualmente importante aqui.
- **Empresa Terceirizada NÃO é escolhida aqui (REGRA DE NEGÓCIO):** diferente de uma primeira versão cogitada, quem decide qual empresa terceirizada vai atender é o **Gestor**, no momento da aprovação (ver item 6) — não o Solicitante. O formulário só registra que a máquina precisa de atendimento terceirizado; a empresa responsável é definida depois.
- **Cadastro de Empresas Terceirizadas (Administrador):** nova entidade simples (`EmpresaTerceirizada`, `/src/tipos/empresaTerceirizada.ts`: `nome`, `especialidade?`, `telefone?`) com CRUD próprio no Painel do Administrador (`AdministradorEmpresasTerceirizadas`/`CadastrarEmpresaTerceirizada`, rota `/administrador/empresas-terceirizadas`), seguindo o mesmo padrão mock-first de `servicoLojas` (ver item 12). É essa lista que alimenta o select do Gestor no `ModalAprovarOSTerceiros` (item 6).
- **Serviço (`servicoOSTerceiros.ts`, tipo `NovaSolicitacaoOSTerceirosPayload` em `/src/tipos/osTerceiros.ts`):** mock-first — constrói uma `SolicitacaoOS` com `tipo: 'terceiros'` (`maquinaNome`/`maquinaCodigo` copiados da máquina selecionada, `tipoDefeito` preservado, `fotoUrl`/`videoUrl` via `URL.createObjectURL`) e empurra para `SOLICITACOES_MOCK` via `gerarProximoIdSolicitacao`, mesmo padrão do item 3b.
- **Integrado ao pipeline de aprovação (REGRA DE NEGÓCIO):** aparece na aba `Solicitações` do Gestor (item 6) igual às demais, mas o botão vira **"Aprovar"** em vez de "Abrir OS" — abre o `ModalAprovarOSTerceiros`, onde o Gestor escolhe a Empresa Terceirizada. Diferente de Maquinário/Reparo, **não passa pelos estados do Técnico**: a `OrdemServico` já nasce `Concluída` (sem Técnico, sem Urgência), indo direto para "Custos Pendentes" do Administrador (item 12/13) — quem executa o reparo é a empresa terceirizada, fora do sistema. Ainda **não aparece** em `Minhas Solicitações` (item 4) — passo futuro, igual ao item 3b.

### 4. Tela de Minhas Solicitações (`MinhasSolicitacoes`)
- **Estrutura:** Layout cinza, barra de pesquisa, filtros em rótulos/badges (`Todos`, `Pendente`, `Convertida`, `Rejeitada`).
- **Listagem & Paginação:** Cards detalhados com número da OS, status e descrição. Integrado ao TanStack Query para paginação otimizada.

### 5. Tela de Cadastro de Máquinas (`CadastrarMaquina` - Administrador)
- **Acesso:** Responsabilidade do Administrador (card "Máquinas" no Painel do Administrador, ver item 12) — **não** faz mais parte do fluxo do Solicitante nem do Painel do Gestor. O Solicitante enxerga apenas as máquinas já cadastradas do seu setor ao abrir uma Nova Solicitação OS (item 3).
- **Modal/Tela:** Envio de foto (área pontilhada) e campos primários.
  - **Estratégia de Envio de Arquivo:** o preview usa `URL.createObjectURL`; o mock persiste a foto como `fotoUrl` na própria `Maquina` (`servicoMaquinas`, mock-first — ver Desvio Deliberado no item 12).
- **Campos Loja/Setor:** o Administrador escolhe livremente a Loja (sem trava, diferente do antigo fluxo do Solicitante) e o Setor entre `setoresDisponiveis`.
- **Integração de Preventivas (REGRA DE NEGÓCIO):** O envio do formulário é bloqueado se não houver **pelo menos uma manutenção preventiva** configurada no mesmo fluxo. Array de preventivas validado via Zod.
- **Edição (`/cadastrar-maquina/:id`):** reaproveita a mesma tela; carrega a máquina (`servicoMaquinas.obterPorId`) e as preventivas já cadastradas para ela (filtrando `servicoPreventivas.listar()` por `maquinaId`), preenchendo o formulário. Ao salvar, `servicoMaquinas.atualizar` **substitui** o conjunto de preventivas da máquina pelo enviado no formulário (não faz merge incremental).

### Modal / Tela de Nova Manutenção Preventiva (`ModalManutencaoPreventiva`)
- **Cabeçalho:** Fundo verde escuro com título "PAINEL DO GESTOR" (em letras miúdas) e "Nova Manutenção Preventiva" (em destaque), com botão de fechar (X) no canto superior direito.
- **Campos do Formulário (Validação Zod obrigatória):**
  - **MÁQUINA \***: Select para escolha da máquina vinculada (`idMaquina` / `Selecionar máquina...`).
  - **DESCRIÇÃO \***: Campo de texto (`textarea`) para descrever o procedimento detalhado da manutenção ("Descreva o procedimento de manutenção...").
  - **INTERVALO (DIAS) \***: Campo numérico indicando a frequência em dias ("Ex: 30").
  - **PRÓXIMA DATA \***: Campo de data (`dd/mm/aaaa`) com seletor de calendário nativo.
  - **Status (Ativa):** Componente de alternância (*switch* / *toggle*) acompanhado do texto descritivo "Preventiva habilitada no sistema".
- **Botões de Ação:** Botão "Cancelar" com borda e fundo neutro, e botão "Salvar" sólido em verde com ícone de confirmação.
- **Abertura Automática de Solicitação ao Vencer (REGRA DE NEGÓCIO):** ao chegar (ou passar) a `proximaData` de uma preventiva **ativa**, o sistema abre automaticamente uma Solicitação de OS para o Gestor aprovar — sem exigir ação do Solicitante. Essa solicitação nasce com `origem: 'preventiva'` (`OrigemSolicitacao`, em `/src/tipos/ordemServico.ts`, junto a `preventivaId` referenciando a preventiva de origem) e aparece com **destaque visual** (`BadgeOrigemPreventiva`, borda/ícone em âmbar) tanto no card da listagem (`CardSolicitacaoGestor`) quanto no `ModalDetalhesSolicitacao`, para o Gestor diferenciar de pedidos abertos por um Solicitante. Em produção isso deve ser um job/cron no back-end; no mock atual, `gerarSolicitacoesPreventivasVencidas` (`/src/utilitarios/gerarSolicitacoesPreventivas.ts`) simula o comportamento comparando `proximaData` com a data atual sempre que `servicoSolicitacoes.listarTodas` é chamado (evitando duplicar solicitação para a mesma preventiva). `PREV-007` em `dadosMockPreventivas.ts` já nasce vencida para demonstrar o comportamento.

### 6. Painel do Gestor (`PainelGestor`)
- **Navegação:** Abas para `Solicitações`, `OS em Andamento`, `OS Finalizadas`, `Manutenção Prev.`.
- **Filtro por Loja e Setor Obrigatório:** Listagem separada visualmente em blocos por Loja (Ex: Cabeçalho divisor "Loja: Loja 2 - Filial Sul") e, dentro de cada bloco, subdividida por Setor caso o escopo do gestor não cubra a loja inteira (Ex: "Setor: Padaria"). Escopos com `setores: 'todos'` exibem a loja sem subdivisão por setor.
- **Ações por Solicitação:** Cada card de solicitação (`CardSolicitacaoGestor`) possui um botão de visualização (ícone de olho) que abre o `ModalDetalhesSolicitacao` — somente leitura, com máquina, loja, setor, solicitante, data/hora, status, descrição, tipo de defeito (quando houver) e os Marcadores de Impacto informados pelo Solicitante. Na aba `Solicitações`, o card também exibe um botão de ação cujo rótulo depende do `tipo` da solicitação (`BadgeTipoOS` mostra Maquinário/Terceiros/Reparo em todo lugar que lista OS): para `maquinario`/`reparo` é **"Abrir OS"** (abre o `ModalAbrirOrdemServico`, item 10, atribuindo Técnico + Urgência); para `terceiros` é **"Aprovar"** (abre o `ModalAprovarOSTerceiros`, escolhendo a Empresa Terceirizada — sem Técnico nem Urgência). Solicitações com `origem: 'preventiva'` recebem destaque visual (ver regra acima) para o Gestor identificá-las rapidamente.
- **Evidências visuais no `ModalDetalhesSolicitacao`:** três blocos de imagem/vídeo, exibidos só quando existirem — **Foto da Máquina (cadastro)**, buscada em `MAQUINAS_MOCK` pelo `maquinaCodigo` (a foto que o Administrador cadastrou pra máquina, item 5); **Foto do Item/Defeito** (`solicitacao.fotoUrl` — rótulo muda pra "Foto do Item" quando `tipo === 'reparo'`, já que ali não existe máquina cadastrada); e **Vídeo do Defeito** (`solicitacao.videoUrl`, na OS de Maquinário e na OS Terceiros, itens 3/3c). É a forma de o Gestor avaliar o problema visualmente antes de aprovar/abrir a OS.
- **Mock com Foto do Defeito:** como a Foto do Defeito (itens 3/3c) foi adicionada depois dos dados de demonstração originais, `dadosMockSolicitacoes.ts` define algumas constantes `FOTO_MOCK_*` (PNG minúsculo em base64, sem depender de link externo) e as aplica a várias solicitações pendentes de Maquinário/Terceiros/Reparo espalhadas pelos três escopos de gestor mockados (`gestor1`, `gestor2`, `regional`) — assim toda demo já mostra o recurso funcionando, sem precisar abrir uma solicitação nova a cada teste.
- **Filtro por Tipo de OS:** um `FiltroTipoOS` (`/src/componentes/FiltroTipoOS.tsx`) fica visível acima das abas `Solicitações`/`OS em Andamento`/`OS Finalizadas`, permitindo restringir a listagem a `Maquinário`, `Terceiros` ou `Pequenos Reparos` (ou "Todos os tipos"). O mesmo componente é reaproveitado nas telas do Administrador (`AdministradorCustosPendentes`/`AdministradorOSFinalizadas`, item 12).
- **Aprovação de OS Terceiros (REGRA DE NEGÓCIO):** ao clicar em "Aprovar", `servicoSolicitacoes.aprovarTerceiros` marca a solicitação como `Convertida` e cria uma `OrdemServico` com `tipo: 'terceiros'` e `empresaTerceirizadaId` já `statusExecucao: 'Concluída'` (sem `tecnicoId`/`urgencia`) — ela nunca passa pela aba "OS em Andamento" nem pelo Painel do Técnico, indo direto para "Custos Pendentes" do Administrador (item 12).
- **Aba "OS em Andamento" (REGRA DE NEGÓCIO — acompanhamento, não controle):** o botão `Pausar` continua sendo do Técnico (ver item 9) — ele é quem está na frente do problema quando falta uma peça, então fazer o Gestor aprovar cada pausa criaria um gargalo. Em vez disso, essa aba dá **visibilidade em tempo real** pro Gestor: lista toda OS com `statusExecucao !== 'Concluída'` (`Aberta`, `Em Andamento` ou `Pausada`) dentro do seu escopo, agrupada por Loja/Setor como as demais abas. Cada card (`CardOSEmExecucao`, `PainelGestor/componentes/`, somente leitura — sem botões de ação) mostra técnico responsável, quando a OS foi aberta/iniciada, horas trabalhadas acumuladas até o momento e — com destaque em âmbar — o motivo da pausa quando `statusExecucao === 'Pausada'`. É a forma de o Gestor auditar pausas sem tirar a autonomia do Técnico no momento do bloqueio.
- **Aba "OS Finalizadas" (REGRA DE NEGÓCIO):** o Gestor só enxerga uma OS como finalizada depois que ela passou por **todas as etapas com sucesso** — `statusExecucao === 'Concluída'` (Técnico encerrou, item 11) **e** `custoManutencao !== undefined` (Administrador já lançou o custo de manutenção, item 12/13). Não é mais baseada em `SolicitacaoOS.status === 'Convertida'` (uma solicitação "Convertida" só significa que uma OS foi aberta para ela — o trabalho pode ainda estar em andamento). A lista vem de `useOrdensServicoTodas` (mesmo hook usado pelo Administrador) filtrada por essa regra e agrupada pelo escopo do Gestor via `agruparPorEscopoGestor` (igual às demais abas). Cada card (`CardOSFinalizada`, `PainelGestor/componentes/`) tem os mesmos botões **Visualizar** (`Eye`) e **Imprimir** (`Printer`) do Administrador, abrindo o `ModalDetalhesOS` compartilhado (`/src/paginas/ModalDetalhesOS/`, ver item 13) com `contexto="Painel do Gestor"`.
- **Ações Rápidas:** O Gestor ficou restrito à operação (aprovar solicitação, abrir OS, acompanhar preventivas) — o único botão em `AcoesRapidas` é `Indicadores` (`/dashboard-gestor`, item 8). Todo cadastro/CRUD de Usuários, Lojas, Setores, Técnicos e Máquinas foi centralizado no Administrador (4º perfil — ver item 12); o Gestor não tem mais acesso a `/cadastrar-usuario`, `/cadastrar-loja` ou `/cadastrar-setor`.
- Setores podem se repetir entre lojas diferentes, cada um como um registro independente por loja (`servicoSetores`, mockado). Os formulários (`CadastrarMaquina`, `NovaSolicitacaoOS`, `CadastrarUsuario`) continuam validando `setor` contra a union estática `setoresDisponiveis`/`Setor` (`/src/tipos/maquina.ts`) — a troca desses `z.enum` para validar contra a lista dinâmica de setores cadastrados é um passo futuro ainda não implementado.

### 7. Tela de Cadastro de Usuário (`CadastrarUsuario` - Administrador)
- **Acesso:** card "Usuários" no Painel do Administrador (item 12), rota `/cadastrar-usuario` (criação) e `/cadastrar-usuario/:id` (edição). É onde o Administrador cria o login de Solicitantes, Técnicos, Gestores e outros Administradores (não há tela de auto-cadastro).
- **Campos do Formulário (Validação Zod obrigatória):**
  - **Perfil (Role) \***: seletor `Solicitante` / `Técnico` / `Gestor` / `Administrador` (reaproveita o `SeletorPerfil`, componente compartilhado usado também na Tela de Login).
  - **Nome \***, **E-mail \***, **Senha \***: campos padrão de identificação/acesso.
  - **Telefone**: opcional.
  - **Loja(s) \***: seleção entre as lojas cadastradas (`LOJAS_MOCK`). Solicitante vincula-se a **exatamente uma** loja (seleção única); Técnico e Gestor podem ser vinculados a **múltiplas lojas** (seleção múltipla). **Não se aplica ao Administrador** (acesso total ao tenant, ver regra no início do documento) — para esse perfil, `CamposAcesso` esconde Loja/Setor/Área e mostra apenas um aviso informativo.
  - **Setor(es)**: **não aparece para os perfis Técnico e Administrador** — Técnico enxerga as OS em que for designado como responsável, independente de setor (ver Painel do Técnico, item 9). Para Solicitante é seleção **única** (o setor onde atua). Para Gestor é seleção **múltipla**, com uma alternância adicional **"Acesso total aos setores"** que, quando ativada, dispensa a seleção manual e equivale a `setores: 'todos'` (o mesmo conceito de `EscopoAcessoGestor` usado no Painel do Gestor) para todas as lojas selecionadas.
  - **Área de Atuação \***: aparece **somente para o perfil Técnico**, em substituição ao campo de Setor(es). Seleção única entre `areasTecnico` (`/src/tipos/tecnico.ts`: `Refrigeração`, `Elétrica`, `Mecânica`, `Hidráulica`, `Máquinas em Geral`). É essa área que aparece junto ao nome do técnico no seletor de "Técnico Responsável" do `ModalAbrirOrdemServico` (ver item 10).
  - **Valor/Hora (R$) \***: aparece **somente para o perfil Técnico**, ao lado de Área de Atuação. É o valor usado para calcular automaticamente o Custo Hora do Técnico no encerramento da OS (ver regra de negócio no item 11).
- **Roteamento interno por perfil (mock):** ao salvar um usuário com perfil Técnico, `servicoUsuarios.criar`/`.atualizar` delegam para `servicoTecnicos` (grava em `TECNICOS_MOCK`, a mesma base usada pelo `ModalAbrirOrdemServico` e pelo login mockado do Técnico); os demais perfis (Solicitante/Gestor/Administrador) são gravados em `USUARIOS_MOCK` (`/src/tipos/usuario.ts`, tipo `Usuario`) via `servicoUsuarios`. Por isso a listagem/edição de Técnicos vive numa tela própria (`AdministradorTecnicos`, item 12), separada da listagem de Usuários.
- **Observação de Modelagem:** O mesmo conjunto de setores/acesso-total selecionado no formulário é aplicado a todas as lojas marcadas nesse cadastro — não há, ainda, configuração de setores distintos por loja dentro de um único cadastro (um Gestor com acesso parcial numa loja e total noutra exigiria editar o usuário depois).

### 8. Painel de Indicadores de Máquinas (`DashboardGestor`)
- **Acesso:** Ação rápida "Indicadores" no Painel do Gestor (`/dashboard-gestor`, `perfis={['gestor']}`).
- **Seletor Dinâmico:** Máquinas agrupadas por Loja e, dentro dela, por Setor, respeitando os escopos de acesso do Gestor — reaproveita `agruparPorEscopoGestor` (mesma função usada no Painel do Gestor, item 6) sobre a listagem completa de `useMaquinas()`.
- **Métricas (via React Query):** `useIndicadoresMaquina(maquinaId)` exibe Horas Parada, MTTR, MTBF e Custo Total da máquina selecionada, além de um gráfico de rosca (paradas por Tipo de Defeito, ver `tiposDefeito` no item 3) e um gráfico de barras mensais (Custo Total, últimos 6 meses).
- **Observação de Modelagem:** as métricas dependem de dados históricos de OS finalizada (datas, custo, tempo de reparo). O `ModalEncerrarOrdemServico` (item 11) grava `dataInicio`/`dataFim`/`horasTrabalhadas`/`custoHoraTecnico`/`defeitoConstatado`/`causaRaiz`/`solucao` automaticamente, e o Administrador lança depois o `custoManutencao` (item 12, Custos Pendentes) — o **Custo Total** de uma OS concluída é `custoHoraTecnico + (custoManutencao ?? 0)`. Ainda assim, `servicoIndicadores`/`dadosMockIndicadores.ts` (`/src/tipos/indicadorMaquina.ts`) continuam gerando indicadores mockados determinísticos por máquina, independentes desses dados — ligar `servicoIndicadores.obterPorMaquina` aos fechamentos reais de `ORDENS_SERVICO_MOCK` (somando `custoHoraTecnico` + `custoManutencao`, e no futuro à API real) é um passo seguinte ainda não implementado.
- **Gráficos:** implementados como SVG/CSS local (`GraficoRosca`, `GraficoBarras` em `DashboardGestor/componentes/`), sem biblioteca de terceiros — consistente com a preferência do projeto por wrappers nativos em vez de dependências extras (ver `api.ts`). Paleta categórica fixa por identidade do tipo de defeito em `coresTipoDefeito.ts`.

### 9. Tela Principal do Técnico (`PainelTecnico`)
- **Acesso:** O login com perfil Técnico resolve um `tecnicoId` mockado (`obterTecnicoLogadoMock` em `dadosMockTecnicos.ts`, guardado em `useEstadoAutenticacao`) e redireciona para `/painel-tecnico` (`ROTA_POR_PERFIL`, rota protegida com `perfis={['tecnico']}`). Cada Técnico só visualiza as OS em que `tecnicoId` bate com o seu (`servicoOrdensServico.listarPorTecnico`) — nunca as de outro técnico. Isso inclui tanto OS de Maquinário quanto de Pequenos Reparos (`tipo: 'reparo'`, ver item 3b) — ambas seguem o mesmo ciclo de vida via Técnico. OS Terceiros (`tipo: 'terceiros'`) **nunca** aparece aqui, pois não tem `tecnicoId` (ver item 3c/6).
- **Abas e Filtros:** `OS em Aberto` (padrão, reúne os status `Aberta` e `Em Andamento`), `Pendentes / Pausadas` (espera de peças, ver regra de negócio abaixo) e `OS Concluídas`.
- **Aba "OS Concluídas" exige custo lançado (REGRA DE NEGÓCIO):** mesma regra de "OS Finalizada" usada no Gestor e no Administrador (item 6/13) — a aba só lista OS com `statusExecucao === 'Concluída'` **e** `custoManutencao !== undefined`. Uma OS que o próprio Técnico acabou de encerrar via `ModalEncerrarOrdemServico` (item 11) some das abas dele até o Administrador lançar o custo de manutenção (item 12) — nesse meio-tempo ela não aparece em nenhuma aba do Painel do Técnico, só nas telas do Administrador/Gestor voltadas a custo pendente.
- **Agrupamento por Setor + Loja:** dentro de cada aba, as OS são separadas visualmente em blocos por combinação de Setor e Loja (ex: "Açougue - Loja 1", "Padaria - Loja 3"), via `agruparPorSetorLoja` (`/src/utilitarios/agruparPorSetorLoja.ts`) + `BlocoSetorLoja`. É um agrupamento só de organização visual (sem regra de controle de acesso, diferente do `agruparPorEscopoGestor` do Gestor), útil porque um mesmo Técnico pode atender múltiplas lojas (`lojasIds` em `Tecnico`, `/src/tipos/tecnico.ts`).
- **Valor/Hora do Técnico:** cada Técnico tem um `valorHora` (R$), definido pelo Administrador no cadastro (item 7/12). Esse valor não é exibido/editável pelo próprio Técnico dentro do Painel do Técnico — ele só é usado internamente pelo `ModalEncerrarOrdemServico` (item 11) para calcular o Custo Hora do Técnico automaticamente.
- **Ciclo de Vida da OS (`StatusExecucaoOS`):** `Aberta` → `Em Andamento` → `Concluída`, com `Pausada` acessível a partir de `Aberta` ou `Em Andamento` a qualquer momento:
  - **Aberta:** OS recém-atribuída pelo Gestor, ainda não iniciada. Card mostra `Pausar` e `Iniciar Atendimento` lado a lado.
  - **Iniciar Atendimento:** grava `dataInicio` automaticamente (`new Date()`) e abre a primeira **sessão de trabalho** (`sessaoAtualInicio = agora`), movendo a OS para `Em Andamento`. Card passa a mostrar `Pausar` e `Finalizar OS` (destaque verde, abre o `ModalEncerrarOrdemServico`, item 11).
  - **Pausar:** abre `ModalPausarOrdemServico` (Zod + textarea obrigatória para o motivo, ex: "aguardando peça"). A OS guarda de qual status veio (`statusAntesDaPausa: 'Aberta' | 'Em Andamento'`) antes de virar `Pausada`, e exibe o motivo no card.
  - **Retomar Atendimento:** volta a OS para o status salvo em `statusAntesDaPausa` (ou `Em Andamento` se não houver) e limpa o motivo da pausa; se o status retomado for `Em Andamento`, abre uma **nova sessão de trabalho** (`sessaoAtualInicio = agora`) — ver regra de negócio abaixo.
  - **Concluída:** somente leitura; o card exibe um botão de visualização (ícone de olho) que abre o `ModalDetalhesEncerramento`, mostrando os dados gravados pelo item 11.
- **Horas Trabalhadas vs. Horas Parada (REGRA DE NEGÓCIO):** são dois relógios independentes.
  - **Horas Parada** (a máquina está indisponível) corre **sem interrupção** desde `dataAbertura` até `dataFim` — não desconta pausas do técnico, porque a máquina continua parada mesmo que ele esteja esperando uma peça. É sempre derivada na hora (`calcularHoras(dataAbertura, dataFim)`, `/src/utilitarios/calcularHoras.ts`), sem campo próprio na OS.
  - **Horas Trabalhadas** (mão-na-massa do técnico) é controlada por um acumulador: `horasTrabalhadasAcumuladas` guarda o total já fechado em sessões anteriores, e `sessaoAtualInicio` marca o início da sessão ativa (`OrdemServico`, `/src/tipos/ordemServico.ts`). **Pausar** fecha a sessão ativa (`horasTrabalhadasAcumuladas += calcularHoras(sessaoAtualInicio, agora)`, `sessaoAtualInicio = undefined`) sem tocar em `dataFim`/Horas Parada; **Retomar** (quando volta para `Em Andamento`) abre uma sessão nova. No encerramento (item 11), `horasTrabalhadas` final = `horasTrabalhadasAcumuladas + calcularHoras(sessaoAtualInicio, agora)`.
- Toda essa movimentação de estado é mock-first, mutando `ORDENS_SERVICO_MOCK` diretamente via `servicoOrdensServico.ts` (`iniciar`/`pausar`/`retomar`/`encerrar`) — não há endpoint real para o domínio de execução de OS ainda, então (diferente de `criar`/`abrirOS` dos itens 3/10) essas ações não chamam `api.ts`, para manter o painel funcional de ponta a ponta sem backend.

### Modal de Pausa de OS (`ModalPausarOrdemServico` - Técnico)
- **Cabeçalho:** mesmo padrão dos demais modais — fundo verde escuro, "PAINEL DO TÉCNICO" em letras miúdas e "Pausar OS · #id" em destaque.
- **Aviso:** deixa explícito que a pausa só afeta o relógio de horas do técnico — o tempo de máquina parada continua contando.
- **Campo:** `Motivo da Pausa *` (`textarea`, Zod obrigatório, ex: "Aguardando peça de reposição do fornecedor.").
- **Botões:** `Cancelar` (neutro) e `Pausar OS` (verde, com ícone).

### 10. Modal de Abertura de OS (`ModalAbrirOrdemServico` - Gestor)
- **Reaproveitado por Maquinário e Pequenos Reparos:** o mesmo modal atende `tipo: 'maquinario'` e `tipo: 'reparo'` (item 3b) — ambos precisam de Técnico + Urgência. `tipo: 'terceiros'` usa um modal próprio (`ModalAprovarOSTerceiros`, item 6), sem Técnico nem Urgência.
- **Nível de Urgência:** Cards coloridos (Baixa/Média/Alta mapeado para `IdUrgencia`).
- **Campos de Controle:**
  - **Data/Hora:** Capturada automaticamente do navegador (`new Date()`) no momento da abertura da OS — não é um campo editável pelo Gestor, apenas exibida como confirmação.
  - **Técnico Responsável:** Select com os técnicos disponíveis para a loja da solicitação (`useTecnicos`/`servicoTecnicos`, filtrado por `lojasIds`), exibindo nome **e área de atuação** (ex: "Roberto Alves — Refrigeração"), definida no cadastro do técnico (ver item 7).
- Os **Marcadores de Impacto** (`Afeta Produção`, `Parada Parcial`, `Retrabalho`) **não** são preenchidos aqui — são informados pelo Solicitante na própria Nova Solicitação OS (ver item 3) e exibidos ao Gestor no `ModalDetalhesSolicitacao` (ver item 6).
- **Desvio deliberado — mock-first:** diferente do que o nome sugere, `servicoSolicitacoes.abrirOS` **não** chama mais `api.ts` real — para o fluxo completo (Gestor aprova → Técnico executa → Administrador lança custo → Finalizada) ser demonstrável de ponta a ponta, ele cria a `OrdemServico` diretamente em `ORDENS_SERVICO_MOCK` (via `servicoOrdensServico.criar`) e marca a `SolicitacaoOS` como `Convertida`, mesmo motivo dos desvios já documentados nos itens 9/11/12.

### Modal de Aprovação de OS Terceiros (`ModalAprovarOSTerceiros` - Gestor)
- **Acesso:** abre ao clicar em "Aprovar" num card de solicitação com `tipo: 'terceiros'` na aba `Solicitações` (item 6) — mesmo padrão visual do `ModalAbrirOrdemServico` (cabeçalho verde, "PAINEL DO GESTOR").
- **Campo único:** **Empresa Terceirizada \*** — select alimentado por `useEmpresasTerceirizadas`, listando as empresas cadastradas pelo Administrador (item 12) com nome e especialidade. Não há Urgência nem Técnico (ver item 3c).
- **Data/Hora:** capturada automaticamente (`new Date()`), mesmo padrão do item 10.
- **Ao salvar:** `servicoSolicitacoes.aprovarTerceiros` (mock-first, mesmo motivo do `abrirOS` acima) cria a `OrdemServico` já com `statusExecucao: 'Concluída'` e marca a solicitação como `Convertida`.

### 11. Modal de Encerramento de OS (`ModalEncerrarOrdemServico` - Técnico)
- **Acesso:** abre a partir do botão `Finalizar OS` do card, disponível apenas quando a OS está `Em Andamento` (ver item 9).
- **Formulário de Execução:** o Técnico **não tem acesso a nenhum campo financeiro** — ele não vê nota fiscal, então o custo de peças/manutenção não é preenchido por ele (ver regra de negócio abaixo). Validação rígida Zod apenas para os campos textuais.
  - `dataInicio` / `dataFim` (período): **não são campos editáveis** — mesmo padrão de captura automática do item 10 (`Data/Hora` do `ModalAbrirOrdemServico`). `dataInicio` é o instante em que o Técnico clicou em "Iniciar Atendimento" (gravado na OS); `dataFim` é capturado (`new Date()`) na abertura do próprio modal. Ambos aparecem como confirmação somente leitura ("Início do Atendimento" / "Término do Atendimento").
  - **Horas Trabalhadas**, **Horas Parada** e **Custo Hora do Técnico**: **calculados e exibidos automaticamente**, sem input do Técnico. Horas Trabalhadas = `horasTrabalhadasAcumuladas + calcularHoras(sessaoAtualInicio, agora)` — ou seja, soma o que já foi fechado em pausas anteriores com a sessão ativa no momento do encerramento (ver regra de negócio do item 9); Horas Parada = `calcularHoras(dataAbertura, agora)`, corrida, sem descontar pausas; Custo Hora do Técnico = Horas Trabalhadas × `valorHora` do técnico logado (buscado em `TECNICOS_MOCK` pelo `tecnicoId` da OS — ver item 9).
  - Áreas de texto amplas: `defeitoConstatado`, `causaRaiz`, `solucao`.
- **Botões:** `Cancelar` (neutro) e `Encerrar OS` (verde, com ícone).
- **Regra de Negócio — Custo de Manutenção separado do Custo Hora do Técnico:** o Técnico não tem acesso às notas fiscais de peças/materiais, então esse valor (`custoManutencao`) **não é preenchido no encerramento**. A OS fica `Concluída` com `custoHoraTecnico` já gravado e `custoManutencao` pendente, até o Administrador lançá-lo posteriormente (ver item 12, Custos Pendentes). O **Custo Total** de uma OS é `custoHoraTecnico + (custoManutencao ?? 0)`.
- Ao salvar, os dados ficam gravados na `OrdemServico` (mock, em `servicoOrdensServico.encerrar`, payload `EncerramentoOrdemServicoPayload` sem `custoManutencao`) e ficam disponíveis para consulta posterior via `ModalDetalhesEncerramento` na aba `OS Concluídas` do Painel do Técnico (que mostra "Pendente de lançamento" quando `custoManutencao` ainda não foi lançado) — evitando que essa informação fique "perdida" depois do encerramento.
- **Desvio deliberado da regra geral de `api.ts`:** diferente dos itens 3/10 (que chamam `api.post` num endpoint real), o encerramento aqui é mock-first (mutação em memória), pelo mesmo motivo do restante do item 9 — ainda não existe backend real para o domínio de execução de OS, e usar `api.ts` quebraria a demonstração (a URL de homologação em `.env` não responde). Trocar para `api.ts` real é um passo futuro, junto com a virada do `servicoIndicadores` (item 8) para consumir esses fechamentos de verdade.

### 12. Painel do Administrador (`PainelAdministrador` - 4º perfil)
- **Perfil:** `administrador`, com acesso total ao tenant (sem escopo de Loja/Setor — ver regra no início do documento). Login redireciona para `/painel-administrador` (`ROTA_POR_PERFIL`, rota protegida com `perfis={['administrador']}`).
- **Home do Painel:** cards de navegação (reaproveitando `CardAcao`, movido de `HomeSolicitante/componentes` para `/src/componentes` por ser usado em mais de uma tela) para `Usuários`, `Lojas`, `Setores`, `Técnicos`, `Máquinas`, `Empresas Terceirizadas`, `Custos Pendentes` e `OS Finalizadas`.
- **CRUD Completo em cada entidade:** Usuários, Lojas, Setores, Técnicos, Máquinas e Empresas Terceirizadas têm telas de listagem próprias (`AdministradorUsuarios`, `AdministradorLojas`, `AdministradorSetores`, `AdministradorTecnicos`, `AdministradorMaquinas`, `AdministradorEmpresasTerceirizadas`) com **barra de busca** (`CampoBusca`) e **filtros** (por perfil/loja/setor conforme a entidade), além de ações de **Editar** (ícone lápis, navega para a tela de cadastro correspondente em modo edição via `/:id`, ou abre `ModalEditarTecnico` no caso de Técnicos) e **Excluir** (ícone lixeira, com confirmação via `ModalConfirmarExclusao`, componente compartilhado). Mobile-First obrigatório também aqui: cada linha da listagem empilha em coluna (`flex-col`) até o breakpoint `sm`, virando linha (`sm:flex-row sm:justify-between`) só a partir daí, com `min-w-0`/`truncate` no texto para não estourar o card em telas estreitas.
- **Empresas Terceirizadas (`AdministradorEmpresasTerceirizadas`/`CadastrarEmpresaTerceirizada`):** entidade simples (`EmpresaTerceirizada`, `/src/tipos/empresaTerceirizada.ts`: `nome`, `especialidade?`, `telefone?`), sem vínculo de Loja/Setor — cadastrada pelo Administrador e consumida pelo Solicitante no select de "Empresa Terceirizada" da OS Terceiros (item 3c).
- **Paginação (Front-end):** cada listagem pagina os resultados já filtrados em blocos de **10 itens por página**, com o componente compartilhado `Paginacao` (`/src/componentes/Paginacao.tsx`, também usado em `MinhasSolicitacoes`). A paginação é client-side (`Array.slice` sobre o array filtrado, sem nova chamada ao `servico*`) e a página volta para 1 sempre que busca/filtros mudam (ajuste feito durante a renderização, comparando uma chave dos filtros com o valor anterior — não em `useEffect`, para não disparar `setState` dentro de efeito).
- **Desvio deliberado — CRUD mock-first:** para o CRUD completo funcionar de ponta a ponta sem backend (mesmo motivo do item 9/11), os serviços `servicoLojas`, `servicoSetores`, `servicoTecnicos`, `servicoUsuarios`, `servicoMaquinas` e `servicoEmpresasTerceirizadas` operam diretamente sobre os arrays mockados (`criar`/`atualizar`/`deletar`/`listar`/`obterPorId`), em vez de chamar `api.ts`. Pelo mesmo motivo, `servicoSolicitacoes.criar` (item 3, Maquinário), `servicoReparos.criar` (item 3b) e `servicoOSTerceiros.criar` (item 3c) também empurram direto para `SOLICITACOES_MOCK`, e `servicoSolicitacoes.abrirOS`/`aprovarTerceiros` (itens 6/10) criam a `OrdemServico` direto em `ORDENS_SERVICO_MOCK` via `servicoOrdensServico.criar` — todo o pipeline de aprovação (Gestor → custo do Administrador → Finalizada) funciona sem depender de nenhum endpoint real. Trocar para endpoints reais é um passo futuro, junto com os demais desvios já documentados nos itens 9/11.
- **Usuários vs. Técnicos (separação de armazenamento):** `servicoUsuarios` mantém apenas os perfis Solicitante/Gestor/Administrador em `USUARIOS_MOCK` (tipo `Usuario`, `/src/tipos/usuario.ts`); Técnicos vivem em `TECNICOS_MOCK` via `servicoTecnicos` (mesma base usada pelo `ModalAbrirOrdemServico` e pelo login mockado do Técnico), por isso têm sua própria tela de listagem/edição (ver item 7 para o motivo dessa separação).
- **Cadastro de Máquinas:** ver item 5 — movido do Solicitante para o Administrador, incluindo a obrigatoriedade de ao menos uma preventiva (regra inalterada).
- **Custos Pendentes (`AdministradorCustosPendentes`):** lista as OS com `statusExecucao === 'Concluída'` e `custoManutencao === undefined` (via `servicoOrdensServico.listarTodas`, novo — retorna todas as OS, não só as de um técnico), com busca por máquina/código, filtro por loja e filtro por `FiltroTipoOS` (Maquinário/Terceiros/Reparo). Cada item tem um botão `Lançar Custo` que abre o `ModalLancarCustoManutencao` (mostra Horas Trabalhadas e Custo Hora do Técnico já gravados como referência, ambos `—` para OS Terceiros, que não passa por Técnico) — ao salvar, `servicoOrdensServico.lancarCustoManutencao` grava `custoManutencao` na OS (payload `LancamentoCustoManutencaoPayload`, `/src/tipos/ordemServico.ts`), completando o Custo Total (ver item 11). Uma OS Terceiros aparece aqui assim que o Gestor aprova (ver item 6), já que nasce `Concluída`.
- **OS Finalizadas (`AdministradorOSFinalizadas`):** tela própria do Administrador para a mesma regra e o mesmo modal do item 13 — ver item 13 para o detalhamento completo (também usado, com dados filtrados pelo escopo, na aba `OS Finalizadas` do Painel do Gestor, item 6).

### 13. OS Finalizadas e Impressão de OS (Administrador + Gestor)
- **Regra de Negócio — o que conta como "finalizada":** uma OS só é considerada finalizada quando passou por **todas as etapas com sucesso**: `statusExecucao === 'Concluída'` (o Técnico encerrou o atendimento, item 11) **e** `custoManutencao !== undefined` (o Administrador já lançou o custo de manutenção, item 12). É o complemento exato de "Custos Pendentes" (item 12): uma OS só sai de "pendente" e entra em "finalizada" depois do lançamento do custo.
- **Duas entradas para a mesma regra:**
  - **Administrador** (`AdministradorOSFinalizadas`, `/administrador/os-finalizadas`): vê **todas** as OS finalizadas do tenant (sem escopo), com busca por máquina/código, filtro por loja e paginação (10/página, item 12).
  - **Gestor** (aba `OS Finalizadas` do `PainelGestor`, item 6): vê apenas as OS finalizadas **dentro do seu escopo** de Loja/Setor, agrupadas por Loja/Setor como as demais abas (`agruparPorEscopoGestor`).
- **`ModalDetalhesOS` (compartilhado, `/src/paginas/ModalDetalhesOS/`):** modal somente leitura reaproveitado pelas duas telas (prop `contexto` troca o rótulo do cabeçalho — "Painel do Administrador" ou "Painel do Gestor") com máquina, loja, setor, solicitante, urgência (quando houver — OS Terceiros não tem), datas de abertura/início/término, Horas Trabalhadas, Custo Hora do Técnico, Custo Manutenção, Custo Total, e os textos de Defeito Constatado/Causa Raiz/Solução gravados pelo Técnico no encerramento (item 11). O campo "Técnico Responsável" vira **"Empresa Terceirizada"** quando `ordemServico.tipo === 'terceiros'` (busca em `EMPRESAS_TERCEIRIZADAS_MOCK` pelo `empresaTerceirizadaId`), já que essa OS não tem Técnico. `BadgeTipoOS` identifica o tipo (Maquinário/Terceiros/Reparo) no cabeçalho. Cada card na listagem tem dois botões: `Eye` (**Visualizar**, abre o modal) e `Printer` (**Imprimir**, abre o modal já disparando a impressão via prop `autoImprimir`).
- **Filtro por Tipo de OS:** tanto `AdministradorOSFinalizadas` quanto a aba `OS Finalizadas` do Gestor (item 6) usam o mesmo `FiltroTipoOS` de Custos Pendentes.
- **Impressão (sem backend/PDF):** resolvida 100% no front. O conteúdo do modal fica dentro de um contêiner com `id="area-impressao-os"`, e uma regra `@media print` global (`index.css`) esconde o resto da aplicação (`body * { visibility: hidden }`) e mostra só esse contêiner — os botões `Fechar`/`Imprimir` e o cabeçalho verde do modal levam `print:hidden`. Essa é uma solução deliberadamente mock/local; gerar um PDF real via backend (como já previsto para impressão de OS em `api.ts`, item da Configuração Base de API) é um passo futuro.
- **Mock com cobertura de escopo:** `dadosMockOrdensServico.ts` garante pelo menos uma OS finalizada (`Concluída` + `custoManutencao` lançado) dentro do escopo de cada gestor mockado (`dadosMockGestores.ts`) — OS #3 (Padaria, Loja 1) para `gestor1`, #12 (Peixaria, Loja 1) para `gestor2`, e #16 (Estoque, Loja 2) para `regional` — para que a aba `OS Finalizadas` nunca apareça vazia nos logins de demonstração padrão.